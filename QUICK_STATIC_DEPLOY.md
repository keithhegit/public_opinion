# 快速部署静态 HTML 前端

## 🎯 问题

Next.js 构建失败，原因是 `package-lock.json` 不同步和版本兼容性问题。

## ✅ 解决方案：使用原始静态 HTML

原始前端 `BettaFish-main/templates/index.html` 是**纯 HTML 文件**，可以直接部署，无需 Next.js。

## 📋 快速部署步骤

### 方法 1: 直接使用 templates 目录（最简单）

1. **在 Cloudflare Pages 中更新配置**

   进入 `bettafish-frontend` 项目设置：

   **根目录**：`BettaFish-main/templates`

   **构建命令**：（留空）

   **构建输出目录**：（留空，或设置为 `.`）

   **Framework preset**：选择 `None` 或 `Plain HTML`

2. **保存并部署**

   Cloudflare Pages 会自动部署 `index.html` 文件

### 方法 2: 创建专门的静态目录

1. **创建目录并复制文件**

   ```bash
   mkdir -p static-frontend
   cp BettaFish-main/templates/index.html static-frontend/index.html
   ```

2. **在 Cloudflare Pages 中配置**

   **根目录**：`static-frontend`

   **构建命令**：（留空）

   **构建输出目录**：（留空）

   **Framework preset**：`None`

3. **提交并推送**

   ```bash
   git add static-frontend/
   git commit -m "Add static HTML frontend"
   git push
   ```

## 🔍 API 地址检查

HTML 中使用的是相对路径 `/api/...`，这意味着：
- 如果 Pages 和 Workers 在同一个域名下，会自动工作
- 如果不在，需要配置 Workers 路由或修改 API 地址

### 检查 Workers 路由配置

确保 Workers 已经配置了正确的路由，能够处理 `/api/*` 请求。

## ⚡ 优势

1. **无需构建**：直接部署，几秒钟完成
2. **无依赖问题**：不需要 npm、Node.js 等
3. **更稳定**：不会因为版本问题失败
4. **易于维护**：直接编辑 HTML 即可

## 🎯 推荐

**强烈推荐使用方法 1**，直接使用 `BettaFish-main/templates` 目录，这是最简单的方案。

