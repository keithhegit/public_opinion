# 修改 Cloudflare Pages 监听分支

## 🎯 目标

将 Cloudflare Pages 的生产分支从 `main` 改为 `stable-before-forum`。

## 📋 操作步骤

### 步骤 1: 进入分支控制设置

1. 在 Cloudflare Pages 项目页面
2. 找到 **"分支控制"** (Branch Control) 部分
3. 点击右侧的 **编辑图标** (✏️)

### 步骤 2: 修改生产分支

1. 在 **"生产分支"** (Production Branch) 下拉菜单中
2. 选择 `stable-before-forum`（或输入分支名称）
3. 确保 **"自动部署"** (Automatic Deployment) 保持 **"已启用"** (Enabled)
4. 点击 **保存** (Save)

### 步骤 3: 验证

修改后，Cloudflare Pages 会：
- ✅ 自动监听 `stable-before-forum` 分支的提交
- ✅ 当有新提交时自动触发构建和部署
- ✅ 使用 `stable-before-forum` 分支的代码

## 🔄 或者：合并到 main 分支

如果你想让 `main` 分支使用稳定版本，可以合并：

```bash
git checkout main
git merge stable-before-forum
git push origin main
```

然后 Cloudflare Pages 会自动部署 `main` 分支的最新代码。

## 📝 注意事项

- 修改分支后，Cloudflare Pages 会立即使用新分支的代码进行构建
- 如果新分支有新的提交，会自动触发部署
- 可以随时切换回 `main` 分支

## ✅ 验证

修改后：
1. 检查构建日志，确认使用的是 `stable-before-forum` 分支
2. 等待构建完成
3. 访问部署 URL 验证功能

