# Cloudflare Pages 构建问题排查

## 🔍 常见构建错误及解决方案

### 错误 1: `@cloudflare/next-on-pages` 版本兼容性问题

**症状**：
- 构建失败，提示 `@cloudflare/next-on-pages` 相关错误
- Next.js 16 可能与某些版本不兼容

**解决方案**：
更新到最新版本或使用兼容版本：

```json
"@cloudflare/next-on-pages": "^1.15.0"
```

或者尝试：

```json
"@cloudflare/next-on-pages": "^1.14.0"
```

### 错误 2: 找不到构建输出目录

**症状**：
- 构建成功但部署失败
- 提示找不到 `.vercel/output/static`

**解决方案**：
检查构建输出目录配置：
- 如果根目录是 `bettafish-frontend`，输出目录应该是 `.vercel/output/static`
- 如果根目录是空的，输出目录应该是 `bettafish-frontend/.vercel/output/static`

### 错误 3: Node.js 版本问题

**症状**：
- 构建失败，提示 Node.js 版本不兼容

**解决方案**：
在 Cloudflare Pages 设置中指定 Node.js 版本：
- 进入"设置" > "运行时"
- 设置 Node.js 版本为 `20.x` 或 `18.x`

### 错误 4: 依赖安装失败

**症状**：
- `npm install` 失败
- 提示某些包无法安装

**解决方案**：
1. 检查 `package.json` 中的依赖版本
2. 确保所有依赖都是有效的
3. 尝试使用 `npm ci` 代替 `npm install`

### 错误 5: TypeScript 编译错误

**症状**：
- TypeScript 类型检查失败
- 构建过程中出现类型错误

**解决方案**：
1. 检查 `tsconfig.json` 配置
2. 确保所有类型定义正确
3. 如果只是类型警告，可以在构建命令中添加 `SKIP_TYPE_CHECK=true`

## 🔧 推荐的构建配置

### 方案 A: 使用根目录（推荐）

**根目录**：`bettafish-frontend`

**构建命令**：
```bash
npm ci && npm run build && npx @cloudflare/next-on-pages
```

**构建输出目录**：`.vercel/output/static`

**Node.js 版本**：`20.x`

### 方案 B: 不使用根目录

**根目录**：（留空）

**构建命令**：
```bash
cd bettafish-frontend && npm ci && npm run build && npx @cloudflare/next-on-pages
```

**构建输出目录**：`bettafish-frontend/.vercel/output/static`

**Node.js 版本**：`20.x`

## 📋 调试步骤

1. **查看完整构建日志**
   - 在 Cloudflare Pages 的"部署"标签页
   - 点击失败的部署
   - 查看完整的构建日志

2. **检查关键步骤**
   - `npm install` 或 `npm ci` 是否成功
   - `npm run build` 是否成功
   - `npx @cloudflare/next-on-pages` 是否成功
   - 输出目录是否存在

3. **本地测试构建**
   ```bash
   cd bettafish-frontend
   npm ci
   npm run build
   npx @cloudflare/next-on-pages
   ```
   检查 `.vercel/output/static` 目录是否生成

## 🚨 如果仍然失败

请提供以下信息：
1. 完整的构建日志（特别是错误部分）
2. 使用的构建配置（根目录、构建命令、输出目录）
3. Node.js 版本设置

