# 访问信息

## 🌐 服务器信息

- **服务器IP**: `14.136.93.109`
- **后端域名**: `api.keithhe.com`
- **本地端口**: `5000`

---

## 🔗 访问方式

### 方式 1: 直接通过 IP 访问

```
http://14.136.93.109:5000
```

### 方式 2: 通过域名访问（如果已配置）

```
http://api.keithhe.com:5000
```

或者如果配置了反向代理（Nginx）：

```
http://api.keithhe.com
```

---

## 🧪 前端搜索功能测试

### 测试步骤：

1. **打开浏览器**，访问：
   ```
   http://14.136.93.109:5000
   ```
   或
   ```
   http://api.keithhe.com:5000
   ```

2. **执行搜索任务**：
   - 在前端界面输入搜索关键词
   - 点击搜索按钮
   - 观察是否正常返回结果

3. **监控日志**（在服务器上）：
   ```bash
   sudo journalctl -u bettafish -f
   ```

---

## 🔧 如果使用 Nginx 反向代理

如果 `api.keithhe.com` 已经配置了 Nginx 反向代理，配置可能类似：

```nginx
server {
    listen 80;
    server_name api.keithhe.com;

    location / {
        proxy_pass http://127.0.0.1:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

然后访问：
```
http://api.keithhe.com
```

---

## ✅ 测试清单

- [ ] 通过 IP 访问前端界面
- [ ] 执行一次搜索任务
- [ ] 检查搜索结果是否正常返回
- [ ] 查看日志确认没有错误
- [ ] 验证 Bocha API 正常工作

---

## 📝 注意事项

1. **防火墙**: 确保端口 5000 已开放
2. **域名解析**: 如果使用域名，确保 DNS 已正确配置
3. **HTTPS**: 如果需要 HTTPS，需要配置 SSL 证书

