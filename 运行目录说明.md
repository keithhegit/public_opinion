# BettaFish 运行目录说明

## 📍 重要：运行目录

**项目根目录**: `D:\Code\Public_Opinion`

这是**所有脚本和命令的基础目录**。

## 🎯 如何确认你在正确目录

### 方法1: 查看当前目录

```powershell
Get-Location
# 或
pwd
```

**应该显示**: `D:\Code\Public_Opinion`

### 方法2: 检查关键文件

```powershell
# 在PowerShell中运行
Test-Path "start-test.ps1"
Test-Path "bettafish-workers"
Test-Path "bettafish-frontend"
```

**如果都返回 `True`，说明你在正确目录。**

## 🚀 运行命令的位置

### ✅ 在根目录运行（`D:\Code\Public_Opinion`）

这些脚本**必须在根目录**运行：

```powershell
# 确保在根目录
cd D:\Code\Public_Opinion

# 运行启动脚本
.\start-test.ps1

# 运行部署脚本
.\deploy.ps1
```

### ✅ 在子目录运行

这些命令**需要进入对应的子目录**：

**Workers API**:
```powershell
# 从根目录进入
cd bettafish-workers
npm run dev

# 或使用完整路径
cd D:\Code\Public_Opinion\bettafish-workers
npm run dev
```

**前端**:
```powershell
# 从根目录进入
cd bettafish-frontend
npm run dev

# 或使用完整路径
cd D:\Code\Public_Opinion\bettafish-frontend
npm run dev
```

## 📂 完整目录结构

```
D:\Code\Public_Opinion\                    ← 项目根目录（运行脚本的地方）
│
├── start-test.ps1                        ← 在这里运行: .\start-test.ps1
├── deploy.ps1                            ← 在这里运行: .\deploy.ps1
├── README.md
│
├── bettafish-workers\                    ← 进入这里: cd bettafish-workers
│   ├── src\
│   ├── package.json
│   ├── wrangler.toml
│   └── npm run dev                       ← 在这里运行
│
└── bettafish-frontend\                   ← 进入这里: cd bettafish-frontend
    ├── app\
    ├── components\
    ├── package.json
    └── npm run dev                       ← 在这里运行
```

## 🎯 快速测试步骤

### Step 1: 切换到根目录

```powershell
cd D:\Code\Public_Opinion
```

### Step 2: 运行启动脚本

```powershell
.\start-test.ps1
```

**或者手动启动**:

**打开终端1**:
```powershell
cd D:\Code\Public_Opinion\bettafish-workers
npm run dev
```

**打开终端2**:
```powershell
cd D:\Code\Public_Opinion\bettafish-frontend
npm run dev
```

## ⚠️ 常见错误

### 错误1: "找不到文件 start-test.ps1"

**原因**: 不在根目录

**解决**:
```powershell
cd D:\Code\Public_Opinion
.\start-test.ps1
```

### 错误2: "找不到模块"

**原因**: 在错误的目录运行npm命令

**解决**:
```powershell
# Workers API
cd D:\Code\Public_Opinion\bettafish-workers
npm run dev

# 前端
cd D:\Code\Public_Opinion\bettafish-frontend
npm run dev
```

## ✅ 验证清单

运行测试前，确认：

- [ ] 当前目录是 `D:\Code\Public_Opinion`
- [ ] 可以看到 `start-test.ps1` 文件
- [ ] 可以看到 `bettafish-workers` 目录
- [ ] 可以看到 `bettafish-frontend` 目录

---

**记住**: 
- **脚本**在**根目录**运行
- **npm命令**在**子目录**运行

