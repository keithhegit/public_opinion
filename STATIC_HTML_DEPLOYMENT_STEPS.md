# 静态 HTML 部署步骤

## ✅ 已完成

1. ✅ 创建了 `static-frontend` 目录
2. ✅ 复制了 `BettaFish-main/templates/index.html` 到 `static-frontend/index.html`
3. ✅ 代码已推送到 GitHub

## 🚀 在 Cloudflare Pages 中配置

### 步骤 1: 进入项目设置

1. 访问: https://dash.cloudflare.com
2. 左侧菜单选择 **Pages**
3. 找到你的 `bettafish-frontend` 项目
4. 点击项目名称进入详情页
5. 点击 **Settings** 标签

### 步骤 2: 更新构建设置

在 **Builds & deployments** 部分：

1. **Root directory**: 设置为 `static-frontend`
2. **Build command**: **留空**（不需要构建）
3. **Build output directory**: **留空**（或设置为 `.`）
4. **Framework preset**: 选择 `None` 或 `Plain HTML`

### 步骤 3: 保存并重新部署

1. 点击 **Save** 保存设置
2. 点击 **Retry deployment** 或等待下一次自动部署
3. 或者手动触发部署：点击 **Deployments** 标签 → **Create deployment**

## ✅ 验证部署

部署成功后：

1. 访问你的 Pages URL（例如：`https://bettafish-frontend.pages.dev`）
2. 检查页面是否正常加载
3. 测试 API 连接（确保 Workers 路由配置正确）

## 🔍 API 地址说明

HTML 中使用的是相对路径 `/api/...`，这意味着：

- 如果 Pages 和 Workers 在同一个域名下，会自动工作
- 如果不在，需要确保 Workers 路由配置正确，能够处理 `/api/*` 请求

## 📝 注意事项

1. **无需构建**：静态 HTML 不需要任何构建过程
2. **无依赖**：不需要 npm、Node.js 等
3. **快速部署**：几秒钟即可完成
4. **易于维护**：直接编辑 `static-frontend/index.html` 即可

## 🎯 如果遇到问题

### 问题 1: 页面显示 404

**解决方案**: 检查根目录设置是否正确为 `static-frontend`

### 问题 2: API 请求失败

**解决方案**: 
- 检查 Workers 路由配置
- 确保 Workers 能够处理 `/api/*` 请求
- 检查 CORS 设置

### 问题 3: 样式或功能异常

**解决方案**: 
- 检查 HTML 文件是否完整
- 检查浏览器控制台是否有错误
- 确保所有外部资源（如 Socket.IO CDN）可以正常加载

