# 静态 HTML 部署方案（替代 Next.js）

## 🔍 问题分析

当前 Next.js 构建失败的原因：
1. `package-lock.json` 与 `package.json` 不同步
2. Next.js 16 与 `@cloudflare/next-on-pages` 版本不兼容
3. 依赖安装复杂，构建时间长

## 💡 替代方案：直接使用静态 HTML

### 方案优势

1. **简单直接**：无需构建过程，直接部署 HTML 文件
2. **快速部署**：Cloudflare Pages 可以直接托管静态文件
3. **无依赖问题**：不需要 npm、Node.js 版本匹配等
4. **易于维护**：直接编辑 HTML 文件即可

### 原始架构

从 `BettaFish-main/templates/index.html` 看，原始前端是：
- **纯 HTML + CSS + JavaScript**
- **使用 Socket.IO 客户端**（通过 CDN）
- **无构建步骤**，可以直接部署

## ✅ 推荐方案：静态 HTML 部署

### 步骤 1: 准备静态文件

1. **复制 HTML 文件**
   ```bash
   mkdir -p static-frontend
   cp BettaFish-main/templates/index.html static-frontend/index.html
   ```

2. **修改 API 地址**（如果需要）
   - 在 HTML 中查找 API 调用
   - 将 `http://localhost:5000` 替换为 `https://bettafish-api-prod.keithhe2021.workers.dev`

### 步骤 2: 在 Cloudflare Pages 配置

**根目录**：`static-frontend`（或你放置 HTML 的目录）

**构建命令**：（留空，不需要构建）

**构建输出目录**：（留空，或设置为 `.`）

**Framework preset**：选择 `None` 或 `Plain HTML`

### 步骤 3: 部署

1. 推送代码到 GitHub
2. Cloudflare Pages 会自动部署静态文件
3. 无需构建，直接可用

## 🔄 如果必须使用 Next.js

### 修复 package-lock.json 问题

在本地运行：

```bash
cd bettafish-frontend
rm package-lock.json
npm install
git add package-lock.json
git commit -m "Update package-lock.json"
git push
```

### 或者使用 npm install 代替 npm ci

在 Cloudflare Pages 配置中，修改构建命令：

**构建命令**：
```bash
cd bettafish-frontend && npm install && npm run build && npx @cloudflare/next-on-pages
```

（使用 `npm install` 代替 `npm ci`，这样即使 lock 文件不同步也能工作）

## 📋 方案对比

| 方案 | 优点 | 缺点 | 推荐度 |
|------|------|------|--------|
| **静态 HTML** | 简单、快速、无依赖问题 | 功能可能受限 | ⭐⭐⭐⭐⭐ |
| **Next.js** | 功能强大、现代化 | 构建复杂、依赖问题多 | ⭐⭐⭐ |

## 🎯 建议

**如果原始前端是纯 HTML**，强烈建议使用静态 HTML 方案：
1. 更简单
2. 更快速
3. 更稳定
4. 更容易维护

**如果确实需要 Next.js 的功能**（如 SSR、路由等），再考虑修复构建问题。

